package org.openehr.rm.demographic;

import org.openehr.rm.common.change_control.VersionedObject;

/**
 * Static type formed by binding generic parameter of `VERSIONED_OBJECT<T>` to `PARTY`.
 */
public interface VersionedParty extends VersionedObject {
}
